{
  "private": true,
  "workspaces": [
    "backend",
    "web-app",
    "admin-dashboard",
    "mobile-app",
    "simulation"
  ],
  "scripts": {
    "format": "prettier --write .",
    "test": "npm test --workspaces",
    "seed": "npm run seed --workspace=simulation"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
