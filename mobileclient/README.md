# Mobile client

## Run Locally

From the repository root:

```bash
npm install
cp mobileclient/.env.example mobileclient/.env
npm run dev --workspace=mobileclient
```

Open [http://localhost:3002](http://localhost:3002)
